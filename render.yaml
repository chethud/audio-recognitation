# Render Blueprint — https://render.com/docs/blueprint-spec
# Deploy: Render Dashboard → New → Blueprint → connect this repo
#
# If you use a manual Web Service instead of Blueprint:
#   Root Directory: (leave EMPTY — not "frontend")
#   Runtime: Docker (recommended) OR Python 3.11 with commands below
services:
  - type: web
    name: alm-lite-api
    runtime: docker
    plan: standard
    region: oregon
    branch: main
    rootDir: .
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: TORCHDYNAMO_DISABLE
        value: "1"
      - key: HF_HOME
        value: /app/.cache/huggingface
      - key: ALM_SUBPROCESS_INFERENCE
        value: "0"
      - key: ALM_ENABLE_CT2
        value: "0"
      - key: ALM_LOW_MEMORY
        value: "1"
      - key: ALM_ALLOW_HF_DOWNLOAD
        value: "1"
      - key: CUDA_VISIBLE_DEVICES
        value: ""
      - key: OMP_NUM_THREADS
        value: "1"
      - key: MKL_NUM_THREADS
        value: "1"
      - key: MALLOC_ARENA_MAX
        value: "2"
      - key: HF_TOKEN
        sync: false
