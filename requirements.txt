# ALM - Audio Language Model
# Core
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
PyYAML>=6.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
imageio-ffmpeg>=0.5.0
moviepy>=1.0.3

# Hugging Face
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
# If TensorFlow is installed: satisfies Transformers' TF Whisper path with Keras 3
tf-keras>=2.15.0

# ALM-Lite: Whisper ASR + AST SED are in transformers
# For smaller footprint use whisper-tiny/base; for best quality use whisper-large-v3
# Stable Kannada ASR on Windows CPU (avoids HF Whisper native crashes / loops)
faster-whisper>=1.0.0

# Utilities
tqdm>=4.65.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Speaker diarization (WhisperX + PyAnnote) — requires Python 3.9–3.13 (use Docker/Python 3.11)
# Accept pyannote/speaker-diarization-3.1 on Hugging Face; set HF_TOKEN in environment.
whisperx>=3.1.1,<3.7.0
pyannote.audio>=3.1.0,<4.0.0
nltk>=3.8.0

# Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
requests>=2.31.0
